using UnityEngine;

namespace Dirigible.SystemsControl
{
    public class DirigibleLightsSystem : MonoBehaviour
    {
        public FluorescentSpotlight frontSpotlight;
        public FluorescentSpotlight rearSpotlight;
        public GameObject leftTubeLight;
        public GameObject rightTubeLight;
        // Start is called once before the first execution of Update after the MonoBehaviour is created
        void Start()
        {
        
        }

        // Update is called once per frame
        void Update()
        {
        
        }
    }
}
